# Landing Base

Proyecto base para una landing page construida con HTML5 y CSS.

## Requisitos

- Un navegador web moderno.
- Git para clonar y gestionar el repositorio.
- Un editor de codigo, como Visual Studio Code.

## Instalacion

1. Clona el repositorio:

   ```bash
   git clone https://github.com/fernandodanielm/my-repo-test.git
   ```

2. Entra en la carpeta del proyecto:

   ```bash
   cd my-project
   ```

## Como ejecutar

Abre `index.html` directamente en el navegador o utiliza la extension Live Server de Visual Studio Code para iniciar un servidor local.

## Desarrolladores

- fernandodanielm

## Convenciones de Ramas

- `develop`: rama de integracion donde se reunen los cambios antes de pasar a `main`.
- `feature/`: prefijo para nuevas funcionalidades o tareas. Cada cambio debe desarrollarse en una rama independiente, por ejemplo `feature/landing-base`.

Para crear una rama de funcionalidad desde `develop`:

```bash
git switch develop
git switch -c feature/nombre-de-la-tarea
```
